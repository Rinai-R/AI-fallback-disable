interface:
  display_name: "AI Fallback Disable"
  short_description: "审计 AI 代码里的错误兜底语义"
  default_prompt: "Use $ai-fallback-disable to review this change for meaningless fallback, wrong fallback, swallowed errors, ambiguous results, and unsafe config or security-boundary defaults."
