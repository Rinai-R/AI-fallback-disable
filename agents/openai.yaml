interface:
  display_name: "AI Fallback Disable"
  short_description: "禁止 AI 配置代码里的无意义兜底"
  default_prompt: "Use $ai-fallback-disable to review this config loader for meaningless fallback, unsafe defaults, secret handling, and startup validation."
