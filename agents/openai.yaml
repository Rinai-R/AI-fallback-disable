interface:
  display_name: "AI Fallback Disable"
  short_description: "审查 AI 代码里的静默兜底与吞错"
  default_prompt: "Use $ai-fallback-disable to review AI-generated code for unsafe fallback, swallowed errors, and unclear failure semantics."
