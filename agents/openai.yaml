interface:
  display_name: "AI Fallback Disable"
  short_description: "优先删除无效 fallback，只保留有明确策略证据的兜底行为"
  default_prompt: "Use $ai-fallback-disable to remove unsupported fallback first, preserve only evidence-backed existing fallback, add none by default, and report the fallback delta."
